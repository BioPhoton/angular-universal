import { SetLangDirective } from './set-lang.directive';

describe('SetLangDirective', () => {
  it('should create an instance', () => {
    const directive = new SetLangDirective();
    expect(directive).toBeTruthy();
  });
});
